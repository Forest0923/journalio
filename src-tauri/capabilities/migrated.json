{
  "identifier": "migrated",
  "description": "permissions that were migrated from v1",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:default",
    "dialog:default",
    "fs:default",
    "fs:allow-app-write",
    {
      "identifier": "fs:default",
      "allow": [{ "path": "$HOME/**" }]
    }
  ]
}
